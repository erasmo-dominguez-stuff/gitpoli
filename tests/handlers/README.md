# Handler TDD Guide

- Write tests for handler normalization and event processing in this folder before implementing handlers.
- Use pytest for input/output assertions.
- Example: test event normalization, input mapping, adapter injection.
- Extract handler logic from tests to handler modules.

## Example

See `test_new_handler.py` for a template.
